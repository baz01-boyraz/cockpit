# dependencies
node_modules

# build output
out
dist/
dist-electron/
/release
*.tsbuildinfo

# iCloud-excluded copies (renamed via the .nosync trick)
*.nosync

# logs
*.log
npm-debug.log*

# local data / secrets
.dev-cockpit/attachments/
.dev-cockpit/secrets/
*.sqlite
*.sqlite3
*.db
.env
.env.*

# screenshots (scratch review artifacts)
temporary screenshots/*.png
# stray review screenshots dumped at the repo root
/*.png

# browser-automation (Playwright MCP) scratch captures
.playwright-mcp/

# memory hub internal churn (auto-snapshots on every note edit, soft-delete trash)
.cockpit-memory/.snapshots/
.cockpit-memory/.trash/

# os
.DS_Store
Thumbs.db

# coverage
coverage/
coverage/
