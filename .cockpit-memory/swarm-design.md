---
schema: 1
name: swarm-design
title: Swarm cards: idle state must carry the redesign
class: architecture
gate: save
updatedAt: 2026-07-06T01:25:59.242Z
---

# Swarm Design

Phase 6. The deliberately-unresolved link from [[memory-hub]] finally resolves —
fitting, because the hub's first real consumer lives here (5.6: workers get
read-only hub context at spawn).

Architecture is **hybrid** (decided in VISION, held in `docs/plans/swarm-plan.md`):
a TS `SwarmService` in main is the conductor — board state, lifecycle, worktrees,
crash-resume, usage checks. Claude is always a *worker*, never self-driving; the
planner is invoked through the same injectable-runner seam [[diff-review]] built.

Load-bearing choices:
- One implicit board per project — a single `kanban_cards` table (V5); the empty
  V1 `agent_sessions` table is dropped, the card row IS the instance registry.
- Only the service moves cards into/out of `in_progress` — spawn and exit are
  facts it owns. Humans own every other transition. Statuses include `parked`
  for graceful usage-limit stops.
- Parallel cards run in `<project>/.cockpit-worktrees/<slug>/` on `swarm/<slug>`
  branches, cap 3 to start; dirty worktrees are refused, never force-deleted
  (the [[security-enforcement]] delete-file spirit).
- Roles (builder/reviewer/scout/planner) × personas (lenses) are one authored
  system prompt; the reviewer-council — N personas, same diff — reuses the
  Phase 4 runner and is the highest-payoff pattern.
- Worker status derives from exit codes + git state, never from parsing model
  prose (the Phase 4 prose-degrade lesson).

Resume rests on Phase 3.3 substrate: `terminal_sessions.command` +
`reconciled_at` + the UUID-checked `claude --resume` path ([[ipc-contract]]
keeps all four bridge legs honest as the `swarm.*` namespace lands).

## Done-signal — closing the lifecycle loop

Workers are INTERACTIVE `claude` sessions (deliberate: the human can keep
talking to a worker mid-card), so the pty rarely exits on its own and
`terminal:exit` alone left cards Running until someone killed the terminal.
The missing half:

- `SwarmDoneSignal.arm()` (called by `startCard` for worktree runs) installs a
  Claude Code **Stop hook** in `<worktree>/.claude/settings.local.json` that
  touches `.cockpit-done` whenever the worker ends a turn. Both files are
  hidden via `.git/info/exclude` so they never dirty the worktree or the diff
  review. A foreign settings.local.json is never clobbered (graceful degrade
  to the manual lifecycle).
- `SwarmService.board()` consumes the sentinel on every read (the panel polls
  while cards run): Running card + spent signal → **In review**, terminal kept
  alive for follow-ups. Late signals for non-Running cards are consumed and
  ignored. This stays inside the "status from facts, never model prose" rule —
  a hook firing is a runtime fact.
- Running cards also show a liveness row ("output 3s ago" / "quiet 2m") fed by
  `swarmActivityStore` — timestamps only, one app-level onData listener,
  2s-coarsened stamps. The board answers "alive or stalled?" without opening
  the terminal.
- (2026-07-04) A swarm-board redesign fails if the added depth/energy only appears on the running (hero) card or on hover — Baz sees the board at rest and reads it as unchanged. Fix applied in v0.1.32: every card shows its identity/depth idle — always-visible gradient border, agent identity plate (builder=ember "B", reviewer=glacier "R", unassigned=silver dot), molten Start/Resume button, per-lane column color identity (TO DO silver, RUNNING ember, IN REVIEW glacier, DONE lime, PARKED violet). Scope stays confined to src/components/swarm/* + src/styles/swarm.css.
- (2026-07-04) Swarm worker output only renders as a LIVE terminal stream; there is no replay of history after the worker's claude process exits. If Baz never opens 'View terminal', written findings from a research-type card (no code changes) are lost. When a worker exits, its card auto-moves to In Review via SwarmService.onWorkerExit (even on error/kill). Workers are told NOT to commit/push — code changes sit as uncommitted diffs in .cockpit-worktrees/<slug>/ for Baz to review. Proposed fixes: inject a 'write findings to FINDINGS.md in the worktree' rule into research-card worker prompts, and/or show last-N terminal lines / worktree diff summary on In Review cards.
- (2026-07-04) The swarm board's agent picker must show ONLY the mythic team (Apollo, Argos, Atlas, Calliope, Huginn, Vulcan) + Unassigned — not every .md in ~/.claude/agents/. Bug: NamedAgentsService loaded every agent file, so ~36 plain Claude Code subagents (code-reviewer, go-build-resolver, gan-*, etc.) polluted the roster. Fix: the parser now flags whether a file carries a `cockpit:` block (`cockpitTagged`); the service lists only cockpit-tagged agents. Also removed the manual role/persona selectors from the card editor entirely — identity comes solely from the agent's own definition (`cockpit.role` / `cockpit.persona`, set on all six). Selecting an agent shows a 'Role & persona come from <agent>'s definition' hint; saving nulls out any legacy manual role/persona. Shipped in v0.1.30 (commit bc63f98).
- (2026-07-04) Swarm worker output only renders as a LIVE terminal stream ('View terminal' on the card); there is no replay of history after the worker's claude process exits. If Baz never opens the terminal, written findings from a research-type card (no code changes) are lost. On exit, the card auto-moves to In Review via SwarmService.onWorkerExit (even on error/kill — never silently disappears). Workers run in an isolated git worktree at .cockpit-worktrees/<slug>/ on a swarm/<slug> branch and are told NOT to commit/push — code changes sit there as uncommitted diffs for Baz to review; his main working dir is untouched. Proposed fixes: inject a 'write findings to FINDINGS.md in the worktree' rule into research-card worker prompts, and/or show last-N terminal lines or a worktree diff summary on In Review cards.
- (2026-07-04) A swarm-board redesign fails Baz's eye if the added depth/energy only appears on the running (hero) card or on hover — he views the board at rest and reads it as unchanged. Fix shipped in v0.1.32: every card carries identity/depth idle — always-visible gradient border + layered shadow, agent identity plate (builder=ember "B", reviewer=glacier "R", unassigned=silver dot), molten gradient Start/Resume button, per-lane column color identity (TO DO silver, RUNNING ember, IN REVIEW glacier, DONE lime, PARKED violet), micro-gauges in top quota chips. Scope confined to src/components/swarm/* + src/styles/swarm.css.
- (2026-07-04) A swarm worker runs as an interactive `claude` session whose terminal does NOT auto-close. Completion is detected via a Claude Code Stop hook written into the worktree that touches a `.cockpit-done` file; the board polls every 5s (SwarmDoneSignal.ts) and moves the card Running → In Review. 'In Review' means "agent paused — review the diff or keep talking," not "finished": Done is a manual drag decision by the human. The Running column is drag-locked (only real spawn/park events move cards there); To Do / In Review / Done / Parked are hand-draggable. Cards intentionally store no output content (swarmActivityStore.ts treats TUI stream as noise) — hence the v0.1.34 board-clarity work added a git diff-stat badge (+X −Y · N files via new review.diffStat IPC, no LLM), an editor Role/Domain legend, and an In-Review hint.
- (2026-07-04) Swarm worker output only renders as a LIVE terminal stream ('View terminal' on the card); there is no replay of history after the worker's claude process exits. If Baz never opens the terminal, written findings from a research-type card (no code changes) are lost. On worker exit the card auto-moves to In Review via SwarmService.onWorkerExit (even on error/kill — never silently disappears). Workers run in an isolated git worktree at .cockpit-worktrees/<slug>/ on a swarm/<slug> branch and are told NOT to commit/push, so code changes sit there as uncommitted diffs for Baz to review while his main working dir stays untouched. Proposed fixes: inject a 'write findings to FINDINGS.md in the worktree' rule into research-card worker prompts, and/or show last-N terminal lines or a worktree diff summary on In Review cards.
- (2026-07-04) A swarm-board redesign fails Baz's eye if the added depth/energy only shows on the running (hero) card or on hover — he views the board at rest and reads it as unchanged. Fix shipped in v0.1.32: every card carries identity/depth idle — always-visible gradient border + layered shadow, agent identity plate (builder=ember "B", reviewer=glacier "R", unassigned=silver dot), molten gradient Start/Resume button, per-lane column color identity (TO DO silver, RUNNING ember, IN REVIEW glacier, DONE lime, PARKED violet), micro-gauges in the top quota chips. Scope confined to src/components/swarm/* + src/styles/swarm.css.
- (2026-07-04) A swarm-board redesign fails Baz's eye if the added depth/energy only appears on the running (hero) card or on hover — he views the board at rest and reads it as unchanged. v0.1.32 fix: every card carries identity/depth idle — always-visible gradient border + layered shadow, agent identity plate (builder=ember "B", reviewer=glacier "R", unassigned=silver dot), molten gradient Start/Resume button; per-lane column color identity (TO DO silver, RUNNING ember, IN REVIEW glacier, DONE lime, PARKED violet); micro-gauges in top quota chips. Scope confined to src/components/swarm/* + src/styles/swarm.css.
- (2026-07-04) A swarm-board redesign fails Baz's eye if the added depth/energy only appears on the running (hero) card or on hover — he views the board at rest and reads it as unchanged. Fix shipped in v0.1.32: every card carries identity/depth idle — always-visible gradient border + layered shadow, agent identity plate (builder=ember "B", reviewer=glacier "R", unassigned=silver dot), molten gradient Start/Resume button, per-lane column color identity (TO DO silver, RUNNING ember, IN REVIEW glacier, DONE lime, PARKED violet), micro-gauges in top quota chips. Scope confined to src/components/swarm/* + src/styles/swarm.css.
- (2026-07-05) A swarm worker runs as an interactive `claude` session whose terminal does NOT auto-close. Completion is detected by a Claude Code Stop hook written into the worktree that touches a `.cockpit-done` file; the board polls every 5s (SwarmDoneSignal.ts) and moves the card Running → In Review. 'In Review' means 'agent paused — review the diff or keep talking', not 'finished' — Done is a manual human drag. The Running column is drag-locked (only real spawn/park events move cards there); To Do / In Review / Done / Parked are hand-draggable. Cards intentionally store NO output content (swarmActivityStore.ts treats the Claude TUI stream as noise) — which is why the board-clarity work added a no-LLM git diff-stat badge (+X −Y · N files via new review.diffStat IPC), an editor Role/Domain legend (Domain is optional), and an In-Review hint. Editor two-dropdown model: Role = what the agent does, Domain = which area it focuses on (optional); named-agent override is an alternative to the pipeline, not additive.
- (2026-07-05) Swarm worker output only renders as a LIVE terminal stream ('View terminal' on the card); there is no replay of history after the worker's claude process exits. If Baz never opens the terminal, written findings from a research-type card (no code changes) are lost. On worker exit the card auto-moves to In Review via SwarmService.onWorkerExit (even on error/kill — never silently disappears). Workers run in an isolated git worktree at .cockpit-worktrees/<slug>/ on a swarm/<slug> branch and are told NOT to commit/push, so code changes sit there as uncommitted diffs for Baz to review while his main working dir stays untouched. Proposed fixes: inject a 'write findings to FINDINGS.md in the worktree' rule into research-card worker prompts, and/or show last-N terminal lines or a worktree diff summary on In Review cards.
- (2026-07-05) A swarm-board redesign fails Baz's eye if the added depth/energy only appears on the running (hero) card or on hover — he views the board at rest and reads it as unchanged. v0.1.32 fix: every card carries identity/depth idle — always-visible gradient border + layered shadow, agent identity plate (builder=ember "B", reviewer=glacier "R", unassigned=silver dot), molten gradient Start/Resume button; per-lane column color identity (TO DO silver, RUNNING ember, IN REVIEW glacier, DONE lime, PARKED violet); micro-gauges in top quota chips. Scope confined to src/components/swarm/* + src/styles/swarm.css.
- (2026-07-05) A swarm-board redesign fails Baz's eye if the added depth/energy only appears on the running (hero) card or on hover — he views the board at rest and reads it as unchanged. Fix shipped in v0.1.32: every card carries identity/depth idle — always-visible gradient border + layered shadow, agent identity plate (builder=ember "B", reviewer=glacier "R", unassigned=silver dot), molten gradient Start/Resume button, per-lane column color identity (TO DO silver, RUNNING ember, IN REVIEW glacier, DONE lime, PARKED violet), micro-gauges in top quota chips. Scope confined to src/components/swarm/* + src/styles/swarm.css.
- (2026-07-05) A swarm worker runs as an interactive `claude` session whose terminal does NOT auto-close. Completion is detected by a Claude Code Stop hook written into the worktree that touches a `.cockpit-done` file; the board polls every 5s (SwarmDoneSignal.ts) and moves the card Running → In Review. 'In Review' means 'agent paused — review the diff or keep talking', not 'finished' — Done is a manual human drag. The Running column is drag-locked (only real spawn/park events move cards there); To Do / In Review / Done / Parked are hand-draggable. Cards intentionally store NO output content (swarmActivityStore.ts treats the Claude TUI stream as noise), which is why board-clarity work added a no-LLM git diff-stat badge (+X −Y · N files via review.diffStat IPC), an editor Role/Domain legend (Domain is optional, named-agent override is an alternative to the pipeline, not additive), and an In-Review hint.
- (2026-07-05) Swarm worker output only renders as a LIVE terminal stream ('View terminal' on the card); there is no replay of history after the worker's claude process exits. If Baz never opens the terminal, written findings from a research-type card (no code changes) are lost. On worker exit the card auto-moves to In Review via SwarmService.onWorkerExit (even on error/kill — never silently disappears). Workers run in an isolated git worktree at .cockpit-worktrees/<slug>/ on a swarm/<slug> branch and are told NOT to commit/push, so code changes sit there as uncommitted diffs for Baz to review while his main working dir stays untouched. Proposed fixes: inject a 'write findings to FINDINGS.md in the worktree' rule into research-card worker prompts, and/or show last-N terminal lines or a worktree diff summary on In Review cards.
- (2026-07-05) A swarm-board redesign fails Baz's eye if the added depth/energy only appears on the running (hero) card or on hover — he views the board at rest and reads it as unchanged. Fix shipped in v0.1.32: every card carries identity/depth idle — always-visible gradient border + layered shadow, agent identity plate (builder=ember "B", reviewer=glacier "R", unassigned=silver dot), molten gradient Start/Resume button, per-lane column color identity (TO DO silver, RUNNING ember, IN REVIEW glacier, DONE lime, PARKED violet), micro-gauges in top quota chips. Scope confined to src/components/swarm/* + src/styles/swarm.css.
- (2026-07-06) A swarm worker runs as an interactive `claude` session whose terminal does NOT auto-close. Completion is detected by a Claude Code Stop hook written into the worktree that touches a `.cockpit-done` file; the board polls every 5s (SwarmDoneSignal.ts) and moves the card Running → In Review. 'In Review' means 'agent paused — review the diff or keep talking', not 'finished' — Done is a manual human drag. The Running column is drag-locked (only real spawn/park events move cards there); To Do / In Review / Done / Parked are hand-draggable. Cards intentionally store NO output content (swarmActivityStore.ts treats the Claude TUI stream as noise), which is why board-clarity work added a no-LLM git diff-stat badge (+X −Y · N files via review.diffStat IPC), an editor Role/Domain legend (Domain is optional, named-agent override is an alternative to the pipeline, not additive), and an In-Review hint.
- (2026-07-06) Swarm worker output only renders as a LIVE terminal stream ('View terminal' on the card); there is no replay of history after the worker's claude process exits. If Baz never opens the terminal, written findings from a research-type card (no code changes) are lost. On worker exit the card auto-moves to In Review via SwarmService.onWorkerExit (even on error/kill — never silently disappears). Workers run in an isolated git worktree at .cockpit-worktrees/<slug>/ on a swarm/<slug> branch and are told NOT to commit/push, so code changes sit there as uncommitted diffs for Baz to review while his main working dir stays untouched. Proposed fixes: inject a 'write findings to FINDINGS.md in the worktree' rule into research-card worker prompts, and/or show last-N terminal lines or a worktree diff summary on In Review cards.
- (2026-07-06) The swarm board's agent picker must show ONLY the mythic team (Apollo, Argos, Atlas, Calliope, Huginn, Vulcan) + Unassigned — not every .md in ~/.claude/agents/. Bug: NamedAgentsService loaded every agent file, so ~36 plain Claude Code subagents (code-reviewer, go-build-resolver, gan-*, etc.) installed from the rules library polluted the roster (dropdown showed ~42 agents). Fix: the parser now flags whether a file carries a `cockpit:` block (`cockpitTagged`); the service lists only cockpit-tagged agents. Non-team subagents stay usable as tools but never reach the board. Also removed the manual role/persona selectors from the swarm card editor entirely — identity comes solely from the agent's own definition (cockpit.role / cockpit.persona, set on all six); selecting an agent shows a 'Role & persona come from <agent>'s definition' hint, and saving nulls out any legacy manual role/persona. Shipped in v0.1.30 (commit bc63f98).
- (2026-07-06) A swarm-board redesign fails Baz's eye if the added depth/energy only appears on the running (hero) card or on hover — he views the board at rest and reads it as unchanged (this is exactly what got v0.1.31's swarm pass rejected). Fix shipped in v0.1.32 (scope confined to src/components/swarm/* + src/styles/swarm.css): every card carries identity/depth while idle — always-visible gradient border + layered shadow, agent identity plate (builder=ember "B", reviewer=glacier "R", unassigned=silver dot), molten-gradient Start/Resume button, per-lane column color identity (TO DO silver, RUNNING ember, IN REVIEW glacier, DONE lime, PARKED violet), and micro-gauges in the top quota chips.
- (2026-07-06) A swarm-board redesign fails Baz's eye if the added depth/energy only appears on the running (hero) card or on hover — he views the board at rest and reads it as unchanged. Fix: every card must carry identity/depth idle — always-visible gradient border + layered shadow, agent identity plate (builder=ember "B", reviewer=glacier "R", unassigned=silver dot), molten gradient Start/Resume button, per-lane column color identity (TO DO silver, RUNNING ember, IN REVIEW glacier, DONE lime, PARKED violet), micro-gauges in top quota chips. Scope confined to src/components/swarm/* + src/styles/swarm.css.
